{{!< default}}

{{!-- Post template - inherits from default.hbs --}}
{{#post}}
<article class="container mx-auto px-4 py-8 max-w-4xl">
    <!-- Breadcrumbs -->
    <div class="breadcrumbs text-sm mb-6">
        <ul>
            <li><a href="{{@site.url}}/blog/" class="hover:text-primary">Home</a></li>
            {{#if primary_tag}}
            <li><a href="{{primary_tag.url}}" class="hover:text-primary">{{primary_tag.name}}</a></li>
            {{/if}}
            <li>{{title}}</li>
        </ul>
    </div>

    <!-- Post Meta -->
    <div class="mb-8">
        {{#if tags}}
        <div class="flex flex-wrap gap-2 mb-4">
            {{#foreach tags limit="3" visibility="public"}}
            <a href="{{url}}" class="badge badge-primary badge-outline hover:badge-primary">{{name}}</a>
            {{/foreach}}
        </div>
        {{/if}}
        
        <h1 class="text-4xl md:text-5xl font-bold mb-4 text-base-content">{{title}}</h1>
        
        <div class="flex items-center gap-4 text-base-content/60 mb-6">
            {{#if primary_author.profile_image}}
            <div class="avatar">
                <div class="w-10 h-10 rounded-full">
                    <img src="{{img_url primary_author.profile_image size="s"}}" alt="{{primary_author.name}}">
                </div>
            </div>
            {{/if}}
            <div>
                <div class="font-semibold text-base-content">{{primary_author.name}}</div>
                <div class="text-sm">
                    <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="MMMM DD, YYYY"}}</time>
                    {{#if @custom.show_reading_time}}
                    <span class="mx-2">•</span>
                    <span>{{reading_time}}</span>
                    {{/if}}
                    {{#if updated_at}}
                    <span class="mx-2">•</span>
                    <span>Updated {{date updated_at format="MMM DD, YYYY"}}</span>
                    {{/if}}
                </div>
            </div>
        </div>

        {{!-- {{#if excerpt}}
        <div class="alert alert-info mb-6">
            <i class="fas fa-info-circle"></i>
            <span class="text-base-content/80">{{excerpt}}</span>
        </div>
        {{/if}} --}}
    </div>

    <!-- Featured Image -->
    {{#if feature_image}}
    <figure class="mb-8">
        <img src="{{img_url feature_image size="xl"}}" alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}" class="w-full rounded-xl shadow-lg">
        {{#if feature_image_caption}}
        <figcaption class="text-center text-sm text-base-content/60 mt-2">{{feature_image_caption}}</figcaption>
        {{/if}}
    </figure>
    {{/if}}

    <!-- Post Content -->
    <div class="prose prose-lg max-w-none text-base-content">
        {{{content}}}
    </div>

    <!-- Post Footer -->
    <div class="mt-12 pt-8 border-t border-base-300">
        <!-- Tags -->
        {{#if tags}}
        <div class="mb-6">
            <h3 class="text-lg font-semibold mb-3 text-base-content">Tags</h3>
            <div class="flex flex-wrap gap-2 max-w-full overflow-hidden">
                {{#foreach tags visibility="public"}}
                <a href="{{url}}" class="badge badge-outline hover:badge-primary whitespace-nowrap flex-shrink-0 text-xs sm:text-sm">{{name}}</a>
                {{/foreach}}
            </div>
        </div>
        {{/if}}

        <!-- Author Bio -->
        {{#if @custom.show_author_bio}}
        <div class="card bg-base-200 mb-8">
            <div class="card-body">
                <div class="flex items-center gap-4">
                    {{#if primary_author.profile_image}}
                    <div class="avatar flex-shrink-0">
                        <div class="w-12 h-12 rounded-full">
                            <img src="{{img_url primary_author.profile_image size="s"}}" alt="{{primary_author.name}}">
                        </div>
                    </div>
                    {{/if}}
                    <div class="flex flex-col">
                        <h3 class="text-xl font-bold text-base-content">{{primary_author.name}}</h3>
                        {{#if primary_author.bio}}
                        <p class="text-base-content/70 mt-1">{{primary_author.bio}}</p>
                        {{/if}}
                        {{#if primary_author.location}}
                        <div class="text-sm text-base-content/60 mt-1">
                            <i class="fas fa-map-marker-alt mr-1"></i>
                            {{primary_author.location}}
                        </div>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
        {{/if}}

        <!-- Share Buttons -->
        {{#if @custom.show_share_buttons}}
        <div class="flex flex-wrap gap-4 justify-center">
            <a href="https://twitter.com/intent/tweet?text={{encode title}}&url={{url absolute="true"}}" target="_blank" class="btn btn-outline">
                <i class="fab fa-twitter mr-2"></i>Share on Twitter
            </a>
            <a href="https://www.linkedin.com/sharing/share-offsite/?url={{url absolute="true"}}" target="_blank" class="btn btn-outline">
                <i class="fab fa-linkedin mr-2"></i>Share on LinkedIn
            </a>
            <button onclick="navigator.share ? navigator.share({title: '{{title}}', url: window.location.href}) : navigator.clipboard.writeText(window.location.href).then(() => alert('Link copied!'))" class="btn btn-outline">
                <i class="fas fa-share mr-2"></i>Share
            </button>
        </div>
        {{/if}}
    </div>
</article>
{{/post}}

<!-- Related Posts -->
{{#if @custom.show_related_posts}}
{{#get "posts" filter="tag:{{primary_tag.slug}}+id:-{{id}}" limit="3" as |related_posts|}}
{{#if related_posts}}
<section class="py-16 bg-base-200">
    <div class="container mx-auto px-4">
        <h2 class="text-3xl font-bold text-center mb-12 text-base-content">Related Posts</h2>
        <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
            {{#foreach related_posts visibility="public"}}
            <article class="card bg-base-100 shadow-xl hover:shadow-2xl transition-shadow">
                {{#if feature_image}}
                <figure>
                    <img src="{{img_url feature_image size="m"}}" alt="{{title}}" class="w-full h-48 object-cover">
                </figure>
                {{else}}
                <figure class="bg-gradient-to-br from-slate-600 to-slate-700 h-48 flex items-center justify-center">
                    <i class="fas fa-file-alt text-4xl text-white"></i>
                </figure>
                {{/if}}
                <div class="card-body">
                    <h3 class="card-title">
                        <a href="{{url}}" class="hover:text-primary">{{title}}</a>
                    </h3>
                    <p class="text-base-content/70">{{excerpt words="15"}}</p>
                    <div class="card-actions justify-between items-center mt-4">
                        <time class="text-sm text-base-content/60">{{date format="MMM DD"}}</time>
                        <a href="{{url}}" class="btn btn-primary btn-sm">Read More</a>
                    </div>
                </div>
            </article>
            {{/foreach}}
        </div>
    </div>
</section>
{{/if}}
{{/get}}
{{/if}}

<!-- Back to Blog -->
<div class="text-center py-8">
    <a href="{{@site.url}}/blog/" class="btn btn-primary">
        <i class="fas fa-arrow-left mr-2"></i>
        Back to Blog
    </a>
</div>