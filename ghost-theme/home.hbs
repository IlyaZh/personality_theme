<!DOCTYPE html>
<html lang="{{@site.locale}}" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0">
    <title>Blog - {{@site.title}}</title>
    <meta name="description" content="{{@site.description}}">
    
           {{!-- Theme styles --}}
      <link rel="stylesheet" type="text/css" href="{{asset "css/screen.css"}}" />
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    {{!-- Favicon --}}
    <link rel="icon" type="image/svg+xml" href="{{asset 'favicon.svg'}}">
    <link rel="alternate icon" href="{{asset 'favicon.ico'}}">
    <link rel="mask-icon" href="{{asset 'favicon.svg'}}" color="#4f46e5">
    
    {{!-- Ghost head --}}
    {{ghost_head}}
</head>
<body class="min-h-screen bg-base-100">
    {{> navigation}}

    <!-- Hero Section -->
    <section class="hero min-h-[40vh] bg-gradient-to-r from-indigo-600 to-purple-600 text-white">
        <div class="hero-content text-center">
            <div class="max-w-md">
                <h1 class="text-5xl font-bold mb-4">My Blog</h1>
                <p class="text-xl mb-6">Sharing experience, knowledge and thoughts about software development</p>
                <div class="stats stats-horizontal bg-white/10 backdrop-blur-sm text-white">
                    <div class="stat">
                        <div class="stat-title text-white/70">Posts</div>
                        <div class="stat-value text-white">{{pagination.total}}</div>
                    </div>
                    <div class="stat">
                        <div class="stat-title text-white/70">Categories</div>
                        <div class="stat-value text-white">4</div>
                    </div>
                    {{!-- <div class="stat">
                        <div class="stat-title text-white/70">Authors</div>
                        <div class="stat-value text-white">{{authors.length}}</div>
                    </div> --}}
                </div>
            </div>
        </div>
    </section>



    <!-- Featured Posts Section -->
    {{#get "posts" filter="featured:true" limit="3" as |featured_posts|}}
    {{#if featured_posts}}
    <section class="py-16 bg-base-200">
        <div class="container mx-auto px-4">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold mb-4 text-base-content">Featured Posts</h2>
                <p class="text-base-content/70 max-w-2xl mx-auto">
                    Highlighted articles and most popular content that you shouldn't miss.
                </p>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
                {{#foreach featured_posts}}
                <article class="card bg-base-100 shadow-xl hover:shadow-2xl transition-all duration-300 hover:scale-105">
                    {{#if feature_image}}
                    <figure class="relative">
                        <img src="{{img_url feature_image size="m"}}" alt="{{title}}" class="w-full h-48 object-cover">
                        <div class="absolute top-4 left-4">
                            <div class="badge badge-primary badge-lg">
                                <i class="fas fa-star mr-1"></i>
                                Featured
                            </div>
                        </div>
                    </figure>
                    {{else}}
                    <figure class="bg-gradient-to-br from-primary to-secondary h-48 flex items-center justify-center relative">
                        <i class="fas fa-file-alt text-6xl text-white opacity-50"></i>
                        <div class="absolute top-4 left-4">
                            <div class="badge badge-accent badge-lg">
                                <i class="fas fa-star mr-1"></i>
                                Featured
                            </div>
                        </div>
                    </figure>
                    {{/if}}
                    
                    <div class="card-body">
                        <div class="flex flex-wrap gap-2 mb-3">
                            {{#foreach tags limit="2" visibility="public"}}
                            <a href="{{url}}" class="badge badge-outline badge-sm hover:badge-primary">{{name}}</a>
                            {{/foreach}}
                        </div>
                        
                        <h3 class="card-title text-lg mb-3 line-clamp-2">
                            <a href="{{url}}" class="hover:text-primary transition-colors">{{title}}</a>
                        </h3>
                        
                        <p class="text-base-content/70 text-sm mb-4 line-clamp-3">{{excerpt words="20"}}</p>
                        
                        <div class="flex items-center justify-between text-sm text-base-content/60 mb-4">
                            <div class="flex items-center gap-2">
                                {{#if primary_author.profile_image}}
                                <div class="avatar">
                                    <div class="w-6 h-6 rounded-full">
                                        <img src="{{img_url primary_author.profile_image size="xs"}}" alt="{{primary_author.name}}">
                                    </div>
                                </div>
                                {{/if}}
                                <span>{{primary_author.name}}</span>
                            </div>
                            <time datetime="{{date format="YYYY-MM-DD"}}">{{date format="MMM DD, YYYY"}}</time>
                        </div>
                        
                        <div class="card-actions justify-between items-center">
                            <div class="text-xs text-base-content/50">
                                {{reading_time}}
                            </div>
                            <a href="{{url}}" class="btn btn-primary btn-sm">
                                Read More
                                <i class="fas fa-arrow-right ml-1"></i>
                            </a>
                        </div>
                    </div>
                </article>
                {{/foreach}}
            </div>
        </div>
    </section>
    {{/if}}
    {{/get}}

    <!-- Blog Posts -->
    <section class="py-20 bg-base-100">
        <div class="container mx-auto px-4">
            <!-- Category Filters -->
            <div class="flex flex-wrap justify-center gap-4 mb-12">
                <a href="{{@site.url}}/blog/" class="btn btn-primary">
                    <i class="fas fa-list mr-2"></i>
                    All Posts
                </a>
                <a href="{{@site.url}}/tag/blog/" class="btn btn-outline">
                    <i class="fas fa-newspaper mr-2"></i>
                    Blog
                </a>
                <a href="{{@site.url}}/tag/growth/" class="btn btn-outline">
                    <i class="fas fa-chart-line mr-2"></i>
                    Growth
                </a>
                <a href="{{@site.url}}/tag/projects/" class="btn btn-outline">
                    <i class="fas fa-tools mr-2"></i>
                    Projects
                </a>
                <a href="{{@site.url}}/tag/thoughts/" class="btn btn-outline">
                    <i class="fas fa-lightbulb mr-2"></i>
                    Thoughts
                </a>
            </div>
            
            {{#if posts}}
            <div class="grid lg:grid-cols-3 md:grid-cols-2 gap-8">
                {{#foreach posts visibility="public"}}
                <article class="card bg-base-100 shadow-xl card-hover">
                    {{#if feature_image}}
                    <figure class="px-6 pt-6">
                        <img src="{{img_url feature_image size="m"}}" alt="{{title}}" class="w-full h-48 object-cover rounded-xl">
                    </figure>
                    {{else}}
                    <figure class="px-6 pt-6">
                        <div class="w-full h-48 bg-gradient-to-br from-slate-600 to-slate-700 rounded-xl flex items-center justify-center">
                            {{#has tag="blog"}}
                                <i class="fas fa-newspaper text-4xl text-white"></i>
                            {{/has}}
                            {{#has tag="growth"}}
                                <i class="fas fa-chart-line text-4xl text-white"></i>
                            {{/has}}
                            {{#has tag="projects"}}
                                <i class="fas fa-tools text-4xl text-white"></i>
                            {{/has}}
                            {{#has tag="thoughts"}}
                                <i class="fas fa-lightbulb text-4xl text-white"></i>
                            {{/has}}
                            {{#unless tags}}
                                <i class="fas fa-file-alt text-4xl text-white"></i>
                            {{/unless}}
                        </div>
                    </figure>
                    {{/if}}
                    <div class="card-body">
                        <div class="flex items-center justify-between mb-2">
                            {{#if tags}}
                            <div class="flex gap-2">
                                {{#foreach tags limit="2" visibility="public"}}
                                {{#if @first}}
                                <div class="badge badge-primary">{{name}}</div>
                                {{else}}
                                <div class="badge badge-outline">{{name}}</div>
                                {{/if}}
                                {{/foreach}}
                            </div>
                            {{/if}}
                            <time class="text-sm text-base-content/60" datetime="{{date format="YYYY-MM-DD"}}">
                                {{date format="MMM DD, YYYY"}}
                            </time>
                        </div>
                        <h2 class="card-title">
                            <a href="{{url}}" class="hover:text-primary">{{title}}</a>
                        </h2>
                        <p class="text-base-content/70 line-clamp-3">{{excerpt words="20"}}</p>
                        <div class="card-actions justify-between items-center mt-4">
                            <div class="flex items-center">
                                {{#if primary_author.profile_image}}
                                <div class="avatar">
                                    <div class="w-6 h-6 rounded-full">
                                        <img src="{{img_url primary_author.profile_image size="xs"}}" alt="{{primary_author.name}}">
                                    </div>
                                </div>
                                {{/if}}
                                <span class="text-sm text-base-content/60 ml-2">{{primary_author.name}}</span>
                                <span class="text-sm text-base-content/40 mx-2">•</span>
                                <span class="text-sm text-base-content/60">{{reading_time}}</span>
                            </div>
                            <a href="{{url}}" class="btn btn-primary btn-sm">Read More</a>
                        </div>
                    </div>
                </article>
                {{/foreach}}
            </div>

            {{!-- Pagination - COMMENTED OUT FOR FUTURE USE --}}
            {{!-- 
            {{#if pagination}}
            <div class="flex justify-center mt-16">
                <div class="join">
                    {{#if pagination.prev}}
                    <a href="{{page_url pagination.prev}}" class="join-item btn">«</a>
                    {{else}}
                    <button class="join-item btn btn-disabled">«</button>
                    {{/if}}
                    
                    {{#foreach pagination.pages}}
                    {{#if current}}
                    <button class="join-item btn btn-active">{{page}}</button>
                    {{else}}
                    <a href="{{page_url page}}" class="join-item btn">{{page}}</a>
                    {{/if}}
                    {{/foreach}}
                    
                    {{#if pagination.next}}
                    <a href="{{page_url pagination.next}}" class="join-item btn">»</a>
                    {{else}}
                    <button class="join-item btn btn-disabled">»</button>
                    {{/if}}
                </div>
            </div>
            {{/if}}
            --}}

            {{!-- SEO-Friendly Simple Pagination --}}
            {{#if pagination}}
            <div class="flex justify-center items-center mt-16">
                <div class="join">
                    <!-- Previous Button -->
                    {{#if pagination.prev}}
                    <a href="{{page_url pagination.prev}}" class="join-item btn btn-outline" title="Previous page" rel="prev">
                        <i class="fas fa-chevron-left mr-1"></i>
                        <span class="hidden sm:inline">Previous</span>
                    </a>
                    {{else}}
                    <button class="join-item btn btn-disabled" title="No previous page">
                        <i class="fas fa-chevron-left mr-1"></i>
                        <span class="hidden sm:inline">Previous</span>
                    </button>
                    {{/if}}
                    
                    <!-- Previous page number -->
                    {{#if pagination.prev}}
                    <a href="{{page_url pagination.prev}}" class="join-item btn btn-outline">{{pagination.prev}}</a>
                    {{/if}}
                    
                    <!-- Current page -->
                    <button class="join-item btn btn-primary" aria-current="page">{{pagination.page}}</button>
                    
                    <!-- Next page number -->
                    {{#if pagination.next}}
                    <a href="{{page_url pagination.next}}" class="join-item btn btn-outline">{{pagination.next}}</a>
                    {{/if}}
                    
                    <!-- Next Button -->
                    {{#if pagination.next}}
                    <a href="{{page_url pagination.next}}" class="join-item btn btn-outline" title="Next page" rel="next">
                        <span class="hidden sm:inline">Next</span>
                        <i class="fas fa-chevron-right ml-1"></i>
                    </a>
                    {{else}}
                    <button class="join-item btn btn-disabled" title="No next page">
                        <span class="hidden sm:inline">Next</span>
                        <i class="fas fa-chevron-right ml-1"></i>
                    </button>
                    {{/if}}
                </div>
            </div>
            
            <!-- Page Info -->
            {{#if pagination.total}}
            <div class="text-center mt-4">
                <p class="text-sm text-base-content/60">
                    Page {{pagination.page}} of {{pagination.pages}} • {{pagination.total}} posts total
                </p>
            </div>
            {{/if}}
            {{/if}}

            {{else}}
            <div class="text-center py-20">
                <i class="fas fa-file-alt text-6xl text-base-content/30 mb-4"></i>
                <h3 class="text-2xl font-bold text-base-content mb-2">No posts yet</h3>
                <p class="text-base-content/70">Check back soon for new content!</p>
            </div>
            {{/if}}
        </div>
    </section>

    <!-- Recommendations Section -->
    {{#match @site.recommendations_enabled}}
    <section class="py-16 bg-base-100">
        <div class="container mx-auto px-4">
            <div class="text-center mb-12">
                <h2 class="text-3xl font-bold mb-4 text-base-content">Recommended Reading</h2>
                <p class="text-base-content/70 max-w-2xl mx-auto">
                    Discover other great blogs and resources that I recommend for software engineers and tech enthusiasts.
                </p>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto mb-8">
                {{#recommendations limit="6"}}
                <div class="card bg-base-200 hover:bg-base-300 transition-colors duration-200 shadow-md hover:shadow-lg">
                    <div class="card-body p-6">
                        <div class="flex items-start gap-4">
                            {{#if favicon}}
                            <div class="avatar">
                                <div class="w-12 h-12 rounded-lg overflow-hidden bg-base-300">
                                    <img src="{{favicon}}" alt="{{title}}" loading="lazy" 
                                         class="w-full h-full object-cover"
                                         onerror="this.parentElement.innerHTML='<div class=\'w-full h-full bg-gradient-to-br from-primary to-secondary flex items-center justify-center text-white font-bold text-lg\'>' + '{{title}}'.charAt(0).toUpperCase() + '</div>'"
                                </div>
                            </div>
                            {{else}}
                            <div class="avatar">
                                <div class="w-12 h-12 rounded-lg bg-gradient-to-br from-primary to-secondary flex items-center justify-center text-white font-bold text-lg" data-title="{{title}}">
                                    ?
                                </div>
                            </div>
                            {{/if}}
                            
                            <div class="flex-1 min-w-0">
                                <h3 class="font-semibold text-base-content mb-1 line-clamp-2">{{title}}</h3>
                                <p class="text-sm text-base-content/60 mb-2">{{readable_url url}}</p>
                                {{#if description}}
                                <p class="text-sm text-base-content/70 line-clamp-2">{{description}}</p>
                                {{/if}}
                            </div>
                        </div>
                        
                        <div class="card-actions justify-end mt-4">
                            <a href="{{url}}" data-recommendation="{{id}}" target="_blank" rel="noopener"
                               class="btn btn-primary btn-sm hover:scale-105 transition-transform duration-200">
                                <i class="fas fa-external-link-alt mr-1"></i>
                                Visit
                            </a>
                        </div>
                    </div>
                </div>
                {{/recommendations}}
            </div>
            
            <!-- Show all recommendations button -->
            <div class="text-center">
                <button data-portal="recommendations" 
                        class="btn btn-outline btn-lg hover:btn-primary transition-all duration-200">
                    <i class="fas fa-list mr-2"></i>
                    Show All Recommendations
                </button>
            </div>
        </div>
    </section>
    {{/match}}

    <!-- Newsletter Section -->
    <section class="py-20 bg-base-200">
        <div class="container mx-auto px-4">
            <div class="max-w-2xl mx-auto text-center">
                <h2 class="text-3xl font-bold mb-4 text-base-content">Stay Updated</h2>
                <p class="text-base-content/70 mb-8">Get notified about new posts and updates. No spam, unsubscribe at any time.</p>
                
                <!-- Ghost Subscription Form -->
                <div class="max-w-md mx-auto">
                    <form data-members-form="subscribe" class="join w-full">
                        <input 
                            class="input input-bordered join-item flex-1" 
                            placeholder="Enter your email" 
                            type="email" 
                            name="email" 
                            required 
                            data-members-email>
                        <button 
                            class="btn btn-primary join-item" 
                            type="submit">
                            <span class="loading loading-spinner loading-xs hidden" data-members-loading></span>
                            <span data-members-label>Subscribe</span>
                        </button>
                    </form>
                    
                    <!-- Success Message -->
                    <div class="alert alert-success mt-4 hidden" data-members-success>
                        <i class="fas fa-check-circle"></i>
                        <span>Great! Check your email for a confirmation link.</span>
                    </div>
                    
                    <!-- Error Message -->
                    <div class="alert alert-error mt-4 hidden" data-members-error>
                        <i class="fas fa-exclamation-triangle"></i>
                        <span data-members-error-message>Something went wrong. Please try again.</span>
                    </div>
                </div>
                
                <p class="text-xs text-base-content/50 mt-4">By subscribing, you agree to receive updates about new posts and software engineering insights.</p>
            </div>
        </div>
    </section>

    {{> footer}}

    {{!-- Theme Toggle Script --}}
    <script>
        // Theme toggle functionality
        const themeToggle = document.getElementById('theme-toggle');
        const themeIcon = document.getElementById('theme-icon');
        const html = document.documentElement;

        // Load saved theme or default to light
        const currentTheme = localStorage.getItem('theme') || 'light';
        html.setAttribute('data-theme', currentTheme);
        updateThemeIcon(currentTheme);

        themeToggle.addEventListener('click', () => {
            const currentTheme = html.getAttribute('data-theme');
            const newTheme = currentTheme === 'light' ? 'dark' : 'light';
            
            html.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            updateThemeIcon(newTheme);
        });

        function updateThemeIcon(theme) {
            if (theme === 'light') {
                themeIcon.className = 'fas fa-moon text-xl';
            } else {
                themeIcon.className = 'fas fa-sun text-xl';
            }
        }

        // Mobile menu close functionality
        function closeMobileMenu() {
            const dropdown = document.querySelector('.dropdown');
            if (dropdown) {
                const checkbox = dropdown.querySelector('[tabindex="0"]');
                if (checkbox) {
                    checkbox.blur();
                }
            }
        }

        // Close mobile menu when clicking on links
        document.querySelectorAll('.dropdown-content a').forEach(link => {
            link.addEventListener('click', closeMobileMenu);
        });

        // Close mobile menu when clicking outside
        document.addEventListener('click', function(event) {
            const dropdown = document.querySelector('.dropdown');
            
            // Don't close if clicking on form elements
            if (event.target.matches('input, textarea, button[type="submit"], .form-control, .form-control *')) {
                return;
            }
            
            if (dropdown && !dropdown.contains(event.target)) {
                closeMobileMenu();
            }
        });

        // Close mobile menu with Escape key
        document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
                closeMobileMenu();
            }
        });

        // Initialize recommendation fallback avatars
        document.querySelectorAll('[data-title]').forEach(function(element) {
            const title = element.getAttribute('data-title');
            if (title && title.length > 0) {
                element.textContent = title.charAt(0).toUpperCase();
            }
        });
    </script>

    <script>
    // Ghost Members Subscription Form
    document.addEventListener('DOMContentLoaded', function() {
        const form = document.querySelector('[data-members-form="subscribe"]');
        if (!form) return;

        const emailInput = form.querySelector('[data-members-email]');
        const submitButton = form.querySelector('button[type="submit"]');
        const loadingSpinner = form.querySelector('[data-members-loading]');
        const submitLabel = form.querySelector('[data-members-label]');
        const successMessage = document.querySelector('[data-members-success]');
        const errorMessage = document.querySelector('[data-members-error]');
        const errorMessageText = document.querySelector('[data-members-error-message]');

        form.addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const email = emailInput.value.trim();
            if (!email) return;

            // Show loading state
            if (loadingSpinner) loadingSpinner.classList.remove('hidden');
            if (submitLabel) submitLabel.textContent = 'Subscribing...';
            if (submitButton) submitButton.disabled = true;
            
            // Hide previous messages
            if (successMessage) successMessage.classList.add('hidden');
            if (errorMessage) errorMessage.classList.add('hidden');

            try {
                const response = await fetch('/members/api/send-magic-link/', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        email: email,
                        emailType: 'subscribe'
                    })
                });

                const result = await response.json();

                if (response.ok) {
                    // Show success message
                    if (successMessage) successMessage.classList.remove('hidden');
                    emailInput.value = '';
                } else {
                    throw new Error(result.errors?.[0]?.message || 'Subscription failed');
                }
            } catch (error) {
                // Show error message
                if (errorMessage) errorMessage.classList.remove('hidden');
                if (errorMessageText) errorMessageText.textContent = error.message;
            } finally {
                // Reset loading state
                if (loadingSpinner) loadingSpinner.classList.add('hidden');
                if (submitLabel) submitLabel.textContent = 'Subscribe';
                if (submitButton) submitButton.disabled = false;
            }
        });
    });
    </script>

    {{!-- Ghost foot --}}
    {{ghost_foot}}
</body>
</html>
